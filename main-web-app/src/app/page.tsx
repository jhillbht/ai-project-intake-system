import AIProjectIntakeForm from '@/components/AIProjectIntakeForm'

export default function Home() {
  return (
    <main className="min-h-screen bg-gray-50">
      <AIProjectIntakeForm />
    </main>
  )
}